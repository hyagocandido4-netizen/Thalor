> Nota: este arquivo é histórico (série antiga pré-Phase 0 closeout). Para o M4 atual da série M pós-W, veja `README_PACKAGE_M4_PORTFOLIO_RISK_APPEND.md` e `docs/PORTFOLIO_RISK_M4.md`.

# Package M4 — Failsafe Kernel

## Goal
Centralize safety prechecks that are currently scattered across shell/runtime.

## Scope
- Kill-switch
- Per-asset circuit breaker
- Runtime control repository
- Precheck snapshot contract
