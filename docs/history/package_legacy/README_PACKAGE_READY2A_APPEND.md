# Package READY-2A — CI regression smoke hotfix

- restore compatibility export `_resolve_live_signals_csv_path` on the legacy root observer shim
- keep direct compatibility for `natbin.usecases.observe_signal_topk_perday` imports as well
- add regression test for the legacy helper import used by CI smoke
