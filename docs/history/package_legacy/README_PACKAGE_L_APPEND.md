## Package L (Runtime App Shell)

A new Python app-shell layer is available:

```powershell
python -m natbin.runtime_app --json
```

Use it to inspect the runtime scope, scoped paths, and available refactor-era runtime capabilities.
