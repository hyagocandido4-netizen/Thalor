# Minimal CI dependencies (kept intentionally small).
# Runtime dependencies live in requirements.txt (includes broker client).

pydantic>=2.7
PyYAML>=6.0.1
pytest>=8.0

pandas>=2.2
numpy>=1.26
pyarrow>=15

rich>=13.7
tqdm>=4.66
pydantic-settings>=2.0,<3
